<!-- Modernized Landing Page for Cubeeto -->
<section class="min-h-screen flex items-center  bg-gradient-to-br from-blue-700 to-blue-500 text-white relative overflow-hidden">
  <!-- SVG Curve at Bottom -->
  <div class="absolute inset-0 pointer-events-none z-0 ">
    <svg class="absolute bottom-0 w-full" viewBox="0 0 1440 320" preserveAspectRatio="none">
      <path fill="#ffffff" fill-opacity="1"
        d="M0,160L60,154.7C120,149,240,139,360,154.7C480,171,600,213,720,202.7C840,192,960,128,1080,101.3C1200,75,1320,85,1380,90.7L1440,96L1440,320L1380,320C1320,320,1200,320,1080,320C960,320,840,320,720,320C600,320,480,320,360,320C240,320,120,320,60,320L0,320Z">
      </path>
    </svg>
  </div>

  <div class="container mx-auto px-6 md:px-12 py-20 z-10 relative grid grid-cols-1 md:grid-cols-2 items-center gap-12">
    <!-- Text Content -->
    <div class="space-y-6 animate-on-scroll">
      <h1 class="text-4xl sm:text-5xl md:text-6xl font-extrabold leading-tight">
        Welcome to <span class="text-yellow-300">Cubeeto</span>
      </h1>
      <p class="text-lg sm:text-xl text-blue-100 max-w-xl">
        Your all-in-one destination for buying, renting, and accessing top-quality educational materials.
      </p>
      <div class="flex flex-wrap gap-4 pt-2">
        <a href="/marketplace"
          class="px-6 py-3 bg-white text-blue-700 font-semibold rounded-full shadow hover:bg-blue-100 transition">
          Explore Marketplace
        </a>
        <a href="/notebank"
          class="px-6 py-3 bg-blue-600 hover:bg-blue-500 text-white border border-white rounded-full shadow transition">
          Browse NoteBank
        </a>
      </div>
    </div>

    <!-- Like Box / Image Card -->
    <div class="relative group">
      <div class="transform rotate-[-2deg] scale-[1.02] bg-white rounded-xl p-1 shadow-2xl transition-transform duration-500 group-hover:rotate-0 group-hover:scale-100">
        <div class="overflow-hidden rounded-xl border-4 border-white">
          <img
            src="https://images.pexels.com/photos/3769138/pexels-photo-3769138.jpeg?auto=compress&cs=tinysrgb&h=750&w=1260"
            alt="Cubeeto Platform"
            class="w-full h-auto object-cover max-h-[480px] transition-transform duration-700 group-hover:scale-105">
        </div>
      </div>
    </div>
  </div>
</section>



<% const sections = [
  { title: 'Marketplace', path: '/marketplace', products: latestMarketplaceProducts, color: 'blue', priceSuffix: '' },
  { title: 'Renting', path: '/renting', products: latestRentingProducts, color: 'orange', priceSuffix: '/day' }
]; %>
<!-- marketplace and rentel -->
<% sections.forEach(section => { %>
<section class="mb-16 px-4 animate-on-scroll py-14  ">
  <div class="flex justify-between items-center mb-6">
    <h2 class="text-3xl font-semibold "><%= section.title %></h2>
    <a href="<%= section.path %>" class="text-<%= section.color %>-600 hover:underline flex items-center">
      View All <i class="fas fa-arrow-right ml-2"></i>
    </a>
  </div>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6 ">
    <% if (section.products && section.products.length > 0) { %>
      <% section.products.forEach(product => { %>
        <a href="/marketplace/details/<%= product._id %>" class="group rounded-xl bg-white shadow-md overflow-hidden hover:shadow-xl transition hover:scale-105">
          <div class="relative w-full h-48 sm:h-56 md:h-64 lg:h-48 xl:h-56 justify-center flex ">
  <img 
    src="<%= product.thumbnail %>" 
    alt="<%= product.name %>" 
    class=" w-52 h-52 sm:h-full object-cover "
    loading="lazy"
  >
  
  <span class="absolute top-2 right-2 bg-<%= section.color %>-600 text-white bg-blue-600 px-3 py-1 text-sm rounded-full shadow">
    ₹<%= product.price %><%= section.priceSuffix %>
  </span>
</div>
          <div class="p-4 space-y-2">
            <h3 class="text-lg font-medium truncate"><%= product.name %></h3>
            <p class="text-sm text-gray-500"><i class="fas fa-map-marker-alt mr-1"></i><%= product.place %></p>
            <span class="inline-block bg-<%= section.color %>-100 text-<%= section.color %>-800 text-xs px-2 py-1 rounded">
              <%= product.category %>
            </span>
          </div>
        </a>
      <% }) %>
    <% } else { %>
      <div class="col-span-4 text-center text-gray-500 py-8">
        <p>No <%= section.title.toLowerCase() %> products available yet.</p>
      </div>
    <% } %>
  </div>
</section>
<% }) %>



<!-- NoteBank Section -->
<section class="mb-16 px-4 animate-on-scroll">
  <div class="flex justify-between items-center mb-6">
    <h2 class="text-3xl font-semibold">NoteBank</h2>
    <a href="/notebank" class="text-blue-600 hover:underline flex items-center">
      View All <i class="fas fa-arrow-right ml-2"></i>
    </a>
  </div>
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
    <% if (popularNotes && popularNotes.length > 0) { %>
      <% popularNotes.forEach(note => { %>
        <div class="rounded-xl bg-white shadow-md hover:shadow-xl transition overflow-hidden">
          <div class="p-6 space-y-4">
            <div class="flex justify-center">
              <i class="fas fa-file-pdf text-red-500 text-4xl"></i>
            </div>
            <h3 class="text-lg font-medium text-center truncate"><%= note.name %></h3>
            <div class="text-center space-y-1">
              <span class="bg-blue-100 text-blue-800 text-xs px-2 py-1 rounded"><%= note.subject %></span>
              <p class="text-sm text-gray-600"><%= note.course %> - <%= note.semester %></p>
            </div>
            <div class="flex justify-between items-center text-sm text-gray-500">
              <span><i class="fas fa-download mr-1"></i><%= note.downloadCount %> downloads</span>
              <a href="/notebank/download/<%= note._id %>" class="btn btn-primary px-3 py-1 text-sm">Download</a>
            </div>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <div class="col-span-4 text-center text-gray-500 py-8">
        <p>No notes available yet.</p>
      </div>
    <% } %>
  </div>
</section>

<!-- Study Kit Section -->
<section class="mb-16 px-4 animate-on-scroll">
  <div class="flex justify-between items-center mb-6">
    <h2 class="text-3xl font-semibold">Study Kit</h2>
    <a href="/studykit" class="text-indigo-600 hover:underline flex items-center">
      View Details <i class="fas fa-arrow-right ml-2"></i>
    </a>
  </div>
  <div class="bg-gradient-to-br from-purple-500 to-indigo-600 rounded-xl text-white p-10 shadow-xl">
    <h3 class="text-2xl md:text-3xl font-bold mb-4">Study Kit - Coming Soon!</h3>
    <p class="text-lg md:text-xl mb-6 max-w-2xl">Our all-in-one study kits are designed to supercharge your academic journey with curated notes, practice materials, and more.</p>
    <a href="/studykit" class="btn bg-white text-indigo-600 hover:bg-indigo-50 px-6 py-3 rounded-lg font-medium">
      Learn More
    </a>
  </div>
</section>

<!-- Why Cubeeto Section -->
<section class="mb-16 px-4 animate-on-scroll">
  <h2 class="text-3xl font-semibold text-center mb-12">Why Choose Cubeeto?</h2>
  <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
    <div class="bg-white rounded-xl text-center p-8 shadow-md hover:shadow-lg transition">
      <div class="inline-block p-4 bg-blue-100 text-blue-600 rounded-full mb-4">
        <i class="fas fa-shopping-cart text-2xl"></i>
      </div>
      <h3 class="text-xl font-bold mb-2">Marketplace</h3>
      <p class="text-gray-600">Seamlessly buy and sell products within your trusted community.</p>
    </div>
    <div class="bg-white rounded-xl text-center p-8 shadow-md hover:shadow-lg transition">
      <div class="inline-block p-4 bg-orange-100 text-orange-600 rounded-full mb-4">
        <i class="fas fa-handshake text-2xl"></i>
      </div>
      <h3 class="text-xl font-bold mb-2">Renting</h3>
      <p class="text-gray-600">Rent or lend essential items with ease and transparency.</p>
    </div>
    <div class="bg-white rounded-xl text-center p-8 shadow-md hover:shadow-lg transition">
      <div class="inline-block p-4 bg-indigo-100 text-indigo-600 rounded-full mb-4">
        <i class="fas fa-book text-2xl"></i>
      </div>
      <h3 class="text-xl font-bold mb-2">NoteBank</h3>
      <p class="text-gray-600">Access and contribute valuable study materials for academic growth.</p>
    </div>
  </div>
</section>

<script>
  document.querySelectorAll('.animate-fade-up')
    .forEach(el => {
      new IntersectionObserver(entries => {
        entries.forEach(e => {
          if (e.isIntersecting) e.target.classList.add('visible');
        });
      }, { threshold: 0.2 }).observe(el);
    });
</script>